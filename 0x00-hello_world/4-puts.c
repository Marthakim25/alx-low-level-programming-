#include <stdio.h>
/**
 * main- Servers as the entry for the program
 *
 * Returns: By default returns zero if no errors otherwise returns non-zero value.
 */
int main(void)
{
 puts(*\programming is like building a multilingual puzzle");
 return (0);
} 
