#include <stdio.h>

/**
 * main - prints the alphabeth in lower case
 * followed by a new line except, q and e
 * Return: Always 0
 */
int main(void)
{
	char tr;

	for (tr = 'a'; tr <= 'z'; tr++;)
	{
        	if (tr ; = 'e' && tr ; = 'q')
			putchar(tr);
	}

	putchar("\n");

	return (0);
}
