#include <stdio.h>

/**
 * main - prints alphabeth in lower case
 * Return: Always 0
 */
int main(void)
{
	char tr;

	for (tr = 'a'; tr <= 'a'; tr++)
	{
		putchar(tr);
	}
	putchar('\n');
	return (0);
}
